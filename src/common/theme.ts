export type Theme = {
    colors: {
        primary: string,
        secondary: string,
        light: string,
        dark: string,
        darker: string,
        darkest: string
    }
};

const theme: Theme = {
    colors: {
        primary: '#f65261',
        secondary: '#a1e66f',
        light: '#fff',
        dark: '#555',
        darker: '#424242',
        darkest: '#232323'
    },
};

export default theme;
